<div class="container">
    <div class="row text-center">
        <h3>{{this.mixed_name}}</h3>
    </div>

    <div class="row">
        <div class="col-md-8">
            <ul>
                {{#each mixed}}
                <li>Name: {{this.mixed_name}}</li>
                <li>Country of Origin: {{this.country}}</li>
                <li>City of Origin: {{this.city_of_origin}}</li>
                {{!-- <li>Proof: {{this.proof}}</li> --}}
                {{/each}}
            </ul>
        </div>

        <div class="col-md-4">
            {{!-- Mixed Drink Image --}}
            {{!-- <img src={{this.mixed_picture}} alt={{this.mixed_name}}> --}}
            
        </div>
        
    </div>

</div>
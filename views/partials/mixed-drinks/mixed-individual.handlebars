<div class="container">
    <div class="row text-center">
        <h3>{{this.mixed_name}}</h3>
    </div>

    <div class="row">
        <div class="col-md-8">
            <ul>
                {{#each mixed}}
                <li>Name: {{this.mixed_name}}</li>
                <li>Country of Origin: {{this.country}}</li>
                <li>City of Origin: {{this.city_of_origin}}</li>
                {{!-- <li>Proof: {{this.proof}}</li> --}}
                {{/each}}
            </ul>
        </div>

        <div class="col-md-4">
            {{!-- Mixed Drink Image --}}
            {{!-- <img src={{this.mixed_picture}} alt={{this.mixed_name}}> --}}
            
        </div>       
    </div>

    <div class="row">
        <button class="btn btn-block mix-list">
            <a href="#">Back to Mixed Drink List</a>
        </button>
        <button class="btn btn-block mix-add">
            <a href="#">Make a Mixed Drink!</a>
        </button>
    </div>

</div>

<script type="text/javascript">
$(document).ready(function() {
    // If there is logic it goes here, otherwise, destroy.

}); // End Doc Ready
</script>